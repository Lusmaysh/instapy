## Installation
```console
# clone to repo
$ git clone https://github.com/Lusmaysh/Termux-IDL

# install the requirement
$ apt install python
$ pip install -r requirements.txt

# To Use
$ cd Termux-IDL
$ python main.py
```
